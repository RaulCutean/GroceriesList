import React from "react";

export const Header = ({ title }) => {
  // const styledHeader = {
  //   backgroundColor: "royalblue",
  //   color: "#fff",
  // };
  return (
    <header>
      <h1>{title}</h1>
    </header>
  );
};
